double	measure_disorder(stack)
{
	// check how many pairs are in wrong order, this will be mistakes
	// count how may pairs we have
	double	disorder;
	int	pairs_in_stack;
	int	mistakes;

	disorder = pairs_in_stack / mistakes;
	return (disorder)
}